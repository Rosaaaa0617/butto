# batch mesh
you should define those files' path in <ExtrudeMeshSave>: sample_parameters.ansa_mpar & sample_quality.ansa_qual 

# snapshot
can't snapshot without gui

# file path
call <ExtrudeMeshSave> where the step file is
total.key will be saved where you call <setValues> is